# Copy this file to .env and update values as needed.
# Vite loads .env, not .env.example, so make sure .env exists during development.
VITE_AUTH_URL=http://127.0.0.1:5023/api
VITE_ADMIN_URL=http://127.0.0.1:3000
